package googlecodejam;

import java.io.BufferedReader;
import java.io.File;
import java.io.FileReader;

public class PrimeiroDesafio {

	public static void main(String[] args) throws Exception {
		final FileReader reader = new FileReader(new File("src/main/resources/A-small-practice.in"));
		final BufferedReader br = new BufferedReader(reader);
		
		int credito = 0;
		int contador = 0;
		int produtos = 0;
		
		String[] precos = null;
		
		String linha = br.readLine();
		
		int caso = 1;
		
		while(linha != null){
			
			if(precos == null && contador != 0 && contador % 3 == 0){
 				precos = linha.split(" ");
				Integer[] precosToInt = new Integer[precos.length];
				for (int i = 0; i < precos.length; i++) {
					precosToInt[i] = Integer.parseInt(precos[i]);
				}
				
				for (int i = 0; i < precosToInt.length; i++) {
					Integer precoI = precosToInt[i];
					for (int j = 0; j < precosToInt.length; j++) {
						Integer precoJ = precosToInt[j];
						
						if(precoI + precoJ == credito){
							System.out.println("Case #" + caso + ": " + i++ + " " + j++);
							caso++;
						}
					}
				}
			}else if(produtos == 0 && contador != 0 && contador % 2 == 0){
				produtos = Integer.parseInt(linha);
			}else if(credito == 0 && contador != 0 && contador % 1 == 0){
				credito = Integer.parseInt(linha);
			}
			
			linha = br.readLine();
			contador++;
		}
		
//		final Stream<String> lines = br.lines();
//		lines.forEach((String l) -> System.out.printf("%s\n", l));
		br.close();
	}

}
